'use client';

import React from 'react';

interface MusicPlayerProps {
  title?: string;
  url?: string;
  enabled?: boolean;
  autoPlay?: boolean;
}

export const MusicPlayer: React.FC<MusicPlayerProps> = () => {
  return null;
};
