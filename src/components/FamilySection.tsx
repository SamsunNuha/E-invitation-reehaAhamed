'use client';

import React from 'react';
import { FamilyMember, ThemeConfig } from '@/lib/types';
import { Dictionary } from '@/lib/dictionary';

interface FamilySectionProps {
  family: FamilyMember[];
  theme: ThemeConfig;
  dict: Dictionary;
}

export const FamilySection: React.FC<FamilySectionProps> = () => {
  return null;
};
